      - name: Upload to PixelDrain
        run: |
          # الرفع والتقاط استجابة السيرفر كاملة
          RESPONSE=$(curl -s -F "file=@downloaded_video.mp4" https://pixeldrain.com/api/file)
          
          # التحقق من نجاح العملية
          SUCCESS=$(echo $RESPONSE | jq -r '.success')
          
          if [ "$SUCCESS" = "true" ]; then
            FILE_ID=$(echo $RESPONSE | jq -r '.id')
            echo "✅ تم الرفع بنجاح!"
            echo "🔗 الرابط المباشر: https://pixeldrain.com/api/file/$FILE_ID"
          else
            echo "❌ فشل الرفع. رسالة الخطأ من الموقع:"
            echo $RESPONSE
            exit 1
          fi
